const { Player } = require("./Player");

const main = () => {
  const playerNumber = 10;
  for (let i = 0; i < playerNumber.length; i++) {}
};
main();

p1.setAnswer(p2.answer);

const TFTT = (p1, p2) => {
  const p1 = new Player(1);
  const p2 = new Player(0);
  const p1 = new PlayerAlgo(1, "TFTT");
  const p1 = new PlayerAlgo(1, "bassam");
};
